<!DOCTYPE html>
<html lang="en-gb">
    <head>
        <title>MyWellbeingApp</title>
        <link rel="stylesheet" href="/styles/stylesheet.css" type="text/css">
    </head>
    <body>
        <header>
            {{>template/appHeader}}
            {{! todo - display landing header if not logged in }}
        </header>
        <nav>
            {{>template/appNavBar}}
        </nav>
        <section class="flex-container centre"><h1>{{title}}</h1></section>
        <main>